// @flow

import React, { Component } from 'react';
import { Image } from './core';

const HeaderTitle = (props: Object) => (
  <Image source={require('../img/sevnt-iso.png')} style={{height: 22, width: 76}} />
);

export default HeaderTitle;